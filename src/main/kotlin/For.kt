fun main() {
    // For Array
    var names: Array<String> = arrayOf("Mahesa","Fajar","Nugraha")

    var total = 0;

    for(name in names) {
        println(name)
        total++
    }
    println("Total perulangan = $total")

    For Range
}